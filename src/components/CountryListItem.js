const CountryListItem = ({country, selectCountry}) => {

    //should hold data on country - and be able to be clicked on and pass data back up

    const handleClick = () => {
        //selectCountry(country);
    }
    
    return (
        <li>One CountryListItem!</li>
    );
}

export default CountryListItem;